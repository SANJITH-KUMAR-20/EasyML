# from Pipelines.training_pipeline import training_pipeline
# from zenml.client import Client
# last_run = Client().get_pipeline("training_pipeline").last_run
# trainer_step = last_run
# tracking_url = trainer_step.run_metadata.get("experiment_tracking_url")
# print(tracking_url)
# training_pipeline(data_path= "Data/olist_customers_dataset.csv")
